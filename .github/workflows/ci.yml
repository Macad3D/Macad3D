name: CI

on: [pull_request, push]
  
permissions:
  checks: write
  
jobs:
  build:
    name: Build
    uses: ./.github/workflows/build.yml

  test:
    name: Run Unit Tests
    needs: [build]
    uses: ./.github/workflows/test.yml
