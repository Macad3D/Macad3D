<Project xmlns="http://schemas.microsoft.com/developer/msbuild/2003">

  <PropertyGroup>
    <MMRootDir Condition="$(MMRootDir) == ''">$([MsBuild]::GetDirectoryNameOfFileAbove($(MSBuildThisFileDirectory), Macad3D.sln))\</MMRootDir>
    <EnableSourceControlManagerQueries>false</EnableSourceControlManagerQueries>
  </PropertyGroup>

  <Import Project="$(MMRootDir)Build\MSBuild\Macad.Common.props" />

</Project>
